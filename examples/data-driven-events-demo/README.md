# Data Driven Events Demo

Legacy Rust-wrapper example. The primary no-Rust package equivalent is
`examples/no-rust-events`.

Edit `assets/game.ron` to try `When` conditions such as score gates, action
presses, and all-enemies-cleared scene changes.
