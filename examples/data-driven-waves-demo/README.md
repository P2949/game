# Data Driven Waves Demo

Legacy Rust-wrapper example. The primary no-Rust package equivalent is
`examples/no-rust-waves`.

Edit `assets/game.ron` to tune timed spawns, enemy tags, and wave-cleared
effects without changing Rust code.
