# Audio Demo

Run with `cargo run -p audio-demo`. It uses generated sounds so no extra assets
are needed; replace them with `.sound(...)` and `.music(...)` WAV files, OGG
files when the `game-starter/ogg` feature is enabled, or MP3 files when the
`game-starter/mp3` feature is enabled and `ffmpeg` is installed.
