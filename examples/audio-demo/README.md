# Audio Demo

Run with `cargo run -p audio-demo`. It uses generated sounds so no extra assets
are needed; replace them with `.sound(...)` and `.music(...)` WAV files, or OGG
files when the `game-starter/ogg` feature is enabled.
